/*import { keystoneContext } from '../keystone';

export async function fetchCarsForLandingPage() {
    return await keystoneContext.db.Car.findMany({
        where: { status: 'available' },
        query: 'id name model price images { url } agency { agencyFullName agencyAddress }'
    });
}
*/